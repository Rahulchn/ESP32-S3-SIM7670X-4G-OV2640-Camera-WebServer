#pragma once

// Copy this file to secrets.h and replace these placeholder values.
// secrets.h is ignored by Git so your credentials are not published.
#define WIFI_SSID "YOUR_WIFI_NAME"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
